# AI dotfiles sync - plan

Goal: sync Claude Code, Codex, and Cursor config across machines.
Status: not started. This file is the carried-over context.

## Decision

Git repo at `~/.dotfiles` + symlinks into the real config locations.
Bootstrap script per platform (`install.ps1`, `install.sh`).
Windows symlinks need Developer Mode on, or run elevated.

Alternative considered: `chezmoi` (better if the machine mix is Windows + macOS,
gives per-machine templating and secret-manager integration).
Rejected for now: plain git is simpler and enough.
Rejected outright: OneDrive/Dropbox sync. Conflicts on concurrent edit,
no history, and it puts credentials in a cloud folder.

## Target structure

```
~/.dotfiles/
  claude/
    CLAUDE.md
    settings.json
    keybindings.json
    agents/
    commands/
    hooks/
    skills/
  codex/
    AGENTS.md
    config.toml
    rules/
    skills/
  cursor/
    settings.json
    keybindings.json
    rules/
    extensions.txt
  install.ps1
  install.sh
  .gitignore
```

## What to sync vs ignore

Rule: hand-authored config syncs. Anything the tool generates, caches,
or authenticates with does not.

### Claude (`~/.claude`)

Sync: `CLAUDE.md`, `settings.json`, `keybindings.json`,
`commands/`, `hooks/`, `skills/`, `agents/`.

Ignore: `.credentials.json`, `settings.local.json`, `settings.json.bak`,
`history.jsonl`, `projects/`, `sessions/`, `session-env/`, `todos/`,
`shell-snapshots/`, `file-history/`, `paste-cache/`, `cache/`, `backups/`,
`downloads/`, `chrome/`, `ide/`, `daemon/`, `daemon.lock`,
`daemon.status.json`, `jobs/`, `plans/`, `memory/`, `telemetry/`,
`stats-cache.json`, `policy-limits.json*`, `remote-settings.json`,
`.last-cleanup`, `.last-update-result.json`, `.caveman-active*`.

`plugins/` is managed by the marketplace repo, not by dotfiles. See below.

### Codex (`~/.codex`)

Sync: `AGENTS.md`, `config.toml`, `rules/`, `skills/`.

Ignore: `auth.json`, `history.jsonl`, `session_index.jsonl`, `sessions/`,
all `*.sqlite` plus their `-shm`/`-wal` siblings (goals, logs, memories,
queue, state, thread_history), `cache/`, `models_cache.json`, `packages/`,
`tmp/`, `.tmp/`, `.sandbox/`, `.sandbox-bin/`, `sandbox.*.log`,
`thread-writer-locks/`, `installation_id`, `cap_sid`, `version.json`,
`.personality_migration`, `.sandbox_migration`.

### Cursor

Not yet inspected. `~/.cursor` was empty on this machine and
`%APPDATA%\Cursor\User` did not exist, so Cursor is probably not installed here.
Confirm the real paths on a machine that has it before writing the symlink list.

Expected locations:
- `%APPDATA%\Cursor\User\settings.json` and `keybindings.json` (Windows)
- `~/Library/Application Support/Cursor/User/...` (macOS)
- `~/.cursor/rules/` for project-agnostic rules
- `~/.cursor/mcp.json` for MCP servers

Ignore: `workspaceStorage/`, `globalStorage/` (except hand-edited files),
`History/`, `logs/`, anything holding a token.

Extensions: do not sync the extension folders. Export a list instead
(`cursor --list-extensions > cursor/extensions.txt`) and reinstall from it.

## Machine drift

Paths differ per machine (`D:\git\...` vs `~/dev/...`). Two fixes:

1. Env vars in the synced settings, never hardcoded absolute paths.
2. A git-ignored local override beside each synced file. Claude Code already
   supports this: `settings.json` syncs, `settings.local.json` does not.

## Plugins

The Quaver marketplace repo (`D:\git\Quaver-Claude-Plugin-Marketplace`)
already solves plugin sync. A new machine runs
`claude plugin marketplace add <url>` once. Do not copy plugin content
into dotfiles, it would drift from the marketplace.

## Secrets

Never in the repo, even a private one. `auth.json` and `.credentials.json`
stay machine-local and get regenerated by logging in.
If templating is needed later, chezmoi can pull from a vault at apply time.

## Bootstrap sketch (Windows)

```powershell
$repo = "$PSScriptRoot\claude"
$dest = "$env:USERPROFILE\.claude"
foreach ($item in @("CLAUDE.md","settings.json","keybindings.json","agents","commands","hooks","skills")) {
    $link = Join-Path $dest $item
    if (Test-Path $link) { Remove-Item $link -Recurse -Force }
    New-Item -ItemType SymbolicLink -Path $link -Target (Join-Path $repo $item) | Out-Null
}
```

Needs the same loop for `codex` into `~/.codex` and `cursor` into the
platform-specific Cursor User dir. The `Remove-Item -Recurse -Force` on an
existing real directory destroys it, so the real script must back up first
instead of deleting blind.

## Next steps

1. Confirm the Cursor config paths on a machine with Cursor installed.
2. `git init` in `~/.dotfiles`, write `.gitignore` from the ignore lists above.
3. Move the real config files in, verify nothing secret came along.
4. Write `install.ps1` and `install.sh` with backup-before-link, not delete-before-link.
5. Test on a second machine from a clean clone.
